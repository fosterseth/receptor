---
- node:
    id: foo
    allowedpeers: bar

- log-level: Debug # reloadable
- trace

- tcp-peer:
    address: localhost:8001

- tcp-peer:
    address: localhost:8002 # reloadable

- control-service:
    service: control
    filename: /tmp/foo.sock

- work-command:
    workType: hello
    command: bash
    params: "-c \"echo hello\""
